PORT=4000
NODE_ENV=production

# MongoDB — uses the 'mongo' service name from docker-compose
MONGODB_URI=mongodb://mongo:27017/open-geoguessr

# Generate a strong secret: openssl rand -base64 48
JWT_SECRET=CHANGE_ME

GOOGLE_MAPS_API_KEY=
MAPILLARY_CLIENT_TOKEN=
